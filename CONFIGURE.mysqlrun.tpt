%[** Template for SWT file 'mysqlrun' expanded by 'configure' **]%
%[** ================================================================ **]%
#!/bin/sh
# Command to invoke mysql to run one command, may return an answer.
# Works best for returning a single column from a single row.
# This command can get the password from .my.cnf if you have only one database.
%[MYSQLPATH]% --defaults-file=sql.cnf  --skip-column-names --silent --execute="$1"
